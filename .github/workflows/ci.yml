name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CORE_REPO: dashflowx/dashflowx-core

jobs:
  verify:
    name: Lint / test / pack inspect
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout auth
        uses: actions/checkout@v4
        with:
          path: dashflowx-auth

      - name: Checkout sibling core for file peer
        uses: actions/checkout@v4
        with:
          repository: ${{ env.CORE_REPO }}
          path: dashflowx-core

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: yarn
          cache-dependency-path: dashflowx-auth/yarn.lock

      - name: Install and build sibling @dashflowx/core
        working-directory: dashflowx-core
        run: |
          yarn install --frozen-lockfile
          yarn build

      - run: yarn install --frozen-lockfile
        working-directory: dashflowx-auth
      - run: yarn test
        working-directory: dashflowx-auth
      - run: yarn lint
        working-directory: dashflowx-auth
      - run: yarn build
        working-directory: dashflowx-auth
      - name: Inspect free tarball (no src/pro)
        working-directory: dashflowx-auth
        run: node scripts/pack-inspect.mjs .
      - name: Inspect pro tarball
        working-directory: dashflowx-auth
        run: node scripts/pack-inspect.mjs auth-pro
